"""
Satellite Pass Predictor -- package containing the tool's logic, split by
responsibility (config, TLE data, propagation, time utilities, visibility,
visualization, reporting). main.py at the repo root orchestrates these
modules; it is intentionally the only place that ties them together.
"""
